# dependencies
node_modules/
/node_modules

# next.js
/.next/
/.next-session2/
/out/

# production
/build
connector-service/dist/
whatsapp-service/dist/

# WhatsApp session credentials — treat like a secret, never commit
whatsapp-service/auth_info/

# env
.env*.local
.env
connector-service/.env

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
# .env.example files are safe templates (no secrets) and should always be
# committed — re-include them despite the broad .env* rule above.
!.env.example
!**/.env.example
